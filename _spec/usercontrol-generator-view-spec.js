'use babel';

import UsercontrolGeneratorView from '../lib/usercontrol-generator-view';

describe('UsercontrolGeneratorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
